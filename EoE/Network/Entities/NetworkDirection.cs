namespace EoE.Network.Entities
{
    /// <summary>
    /// Indicates the sending direction of the packet.
    /// </summary>
    public enum NetworkDirection
    {
        Server2Client,
        Client2Server
    }
}
